import { describe, it } from "vitest";

describe('<jar-page>', () => {

  describe('render', () => {

    it('renders page controls', () => {
      
    })

    it('renders jar grid', () => {
      
    })
  })

  describe('import', () => {
    it('updates jar grid on load', () => {
      
    })

    it('updates jar grid on later call to import', () => {
      
    })
  })

  describe('export', () => {

    it('exports all data from grid', () => {
      
    })
  })
  
})
